class Solution {
    public int[] searchRange(int[] nums, int target) {

        List<Integer> l = new ArrayList<>();

        for(int n : nums)
            l.add(n);

        int ans[] = new int[2];
        ans[0] = l.indexOf(target);
        ans[1] = l.lastIndexOf(target);

        return ans;
    }
}
